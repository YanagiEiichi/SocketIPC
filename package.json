{
  "name": "SocketIPC",
  "version": "0.2.3",
  "description": "Implements IPC with Socket on cluster mode",
  "main": "index.js",
  "directories": {
    "test": "tests"
  },
  "scripts": {
    "lint": "eslint .",
    "test": "eslint . && cd tests && ./test.sh"
  },
  "author": "admin@web-tinker.com",
  "license": "MIT",
  "dependencies": {
    "FrameGenerator": "^0.2.4"
  },
  "devDependencies": {
    "eslint": "^3.17.1",
    "eslint-config-elemefe": "^0.2.0"
  }
}
